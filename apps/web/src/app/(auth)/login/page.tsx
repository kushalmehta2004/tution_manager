export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-slate-100 p-6">
      <section className="w-full max-w-md rounded-lg bg-white p-6 shadow-sm">
        <h1 className="text-xl font-semibold text-slate-900">Teacher Login</h1>
        <p className="mt-2 text-sm text-slate-600">
          Auth API is ready in Phase 0. UI form wiring will be completed in the next phase.
        </p>
      </section>
    </main>
  );
}
